# ==============================================================================
# 削除対象リスト (remove-target.txt)
# ==============================================================================
# Salesforceから「削除（Remove）」したいファイルのパスを1行ずつ記述してください。
# 空行や、行頭に '#' がある行は無視されます。
#
# 【記述例（VS Codeの「相対パスのコピー」を使うと便利です）】
# ---------------------------------------------------------
# ▼ 不要になったApexクラスの削除
# force-app/main/default/classes/OldUnusedController.cls
#
# ▼ 不要になったカスタム項目の削除（オブジェクト全体ではなく項目単位で消せます）
# force-app/main/default/objects/Account/fields/ObsoleteField__c.field-meta.xml
#
# ▼ 不要になったLWCコンポーネントの削除
# force-app/main/default/lwc/oldDeprecatedComponent
# ---------------------------------------------------------

